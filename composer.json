{
    "name": "eustasy/authenticatron",
    "description": "HOTP / TOTP secrets with corresponding QR links and code verification from a simple PHP script.",
    "keywords": "security authentication secret totp passwords two-factor-authentication 2fa webauthn",
    "autoload": {
        "classmap": [
            "src/",
            "lib/",
            "_libs/",
            "authenticatron.php"
        ]
    },
    "require-dev": {
        "phpunit/phpunit": "11"
    },
    "scripts": {
        "test": [
            "phpunit",
            "@php authenticatron.php"
        ]
    }
}
